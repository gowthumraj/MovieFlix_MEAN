/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AdminAddChennaiComponent } from './admin-add-chennai.component';

describe('AdminAddChennaiComponent', () => {
  let component: AdminAddChennaiComponent;
  let fixture: ComponentFixture<AdminAddChennaiComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AdminAddChennaiComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AdminAddChennaiComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
