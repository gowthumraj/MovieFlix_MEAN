/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AdminEditChennaiComponent } from './admin-edit-chennai.component';

describe('AdminEditChennaiComponent', () => {
  let component: AdminEditChennaiComponent;
  let fixture: ComponentFixture<AdminEditChennaiComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AdminEditChennaiComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AdminEditChennaiComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
