/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AdminChennaiMoviesComponent } from './admin-chennai-movies.component';

describe('AdminChennaiMoviesComponent', () => {
  let component: AdminChennaiMoviesComponent;
  let fixture: ComponentFixture<AdminChennaiMoviesComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AdminChennaiMoviesComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AdminChennaiMoviesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/