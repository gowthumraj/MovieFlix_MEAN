/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TheatreCompComponent } from './theatre-comp.component';

describe('TheatreCompComponent', () => {
  let component: TheatreCompComponent;
  let fixture: ComponentFixture<TheatreCompComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TheatreCompComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(TheatreCompComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
